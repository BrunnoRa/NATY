<Application x:Class="Naty.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Application.Resources>
    <SolidColorBrush x:Key="BgMain" Color="#050914" />
    <SolidColorBrush x:Key="BgPanel" Color="#071426" />
    <SolidColorBrush x:Key="BgCard" Color="#0B192D" />
    <SolidColorBrush x:Key="Cyan" Color="#18BFFF" />
    <SolidColorBrush x:Key="CyanLight" Color="#26D9FF" />
    <SolidColorBrush x:Key="Purple" Color="#7247FF" />
    <SolidColorBrush x:Key="Success" Color="#31E6A1" />
    <SolidColorBrush x:Key="Warning" Color="#FF8F5C" />
    <SolidColorBrush x:Key="Error" Color="#FF5576" />
    <SolidColorBrush x:Key="TextPrimary" Color="#EAF4FF" />
    <SolidColorBrush x:Key="TextSecondary" Color="#8CA6C5" />
    <SolidColorBrush x:Key="TextMuted" Color="#506985" />
    <SolidColorBrush x:Key="Border" Color="#17466C" />
    <Style TargetType="TextBlock">
      <Setter Property="FontFamily" Value="Segoe UI Variable, Segoe UI" />
      <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
      <Setter Property="Background" Value="{StaticResource BgCard}" />
      <Setter Property="BorderBrush" Value="{StaticResource Border}" />
      <Setter Property="Padding" Value="13,8" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Surface" CornerRadius="8" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Surface" Property="BorderBrush" Value="{StaticResource Cyan}" />
                <Setter TargetName="Surface" Property="Background" Value="#102641" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Surface" Property="Background" Value="#163455" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource BgCard}" />
      <Setter Property="BorderBrush" Value="{StaticResource Border}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="14" />
      <Setter Property="Padding" Value="16" />
    </Style>
  </Application.Resources>
</Application>
